---
title: Reboot
---

# Reboot


To enable this plugin just include the `plugin-reboot()` in the `$plugins` section of your configuration:

```scss
@use "siimple" as siimple;

@include siimple.configure(
    $plugins: (
        siimple.plugin-reboot(),
    ),
);
```

