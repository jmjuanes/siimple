---
title: "Label"
description: "Style your form labels"
---

# Label

Style your form **label** element adding a `siimple-label` class, useful with radio, checkbox and switch elements.

```html live=true
<label for="my-checkbox" class="siimple-label">
    <input type="checkbox" class="siimple-checkbox has-mr-3" id="my-checkbox" />
    I agree with the terms of service.
</label>
```

### Variants

Variants for this component can be defined in the `form.label` section of the variants configuration:

```scss
@use "siimple" with (
    $variants: (
        "form.label": (
            "default": (
                "color": "inherit",
                "font-weight": "bold",
            ),
        ),
    ),
);
```
