---
title: "Select"
description: "A basic dropdown selection element"
---

# Select

You can style your `<select>` elements adding a `siimple-select` class.

```html live=true
<select class="siimple-select">
    <option selected>Option 1</option>
    <option>Option 2</option>
    <option>Option 3</option>
</select>
```


### Variants

Variants for this component can be defined in the `form.select` section of the theme configuration:

```scss
siimple.theme((
    "form.select": (
        "default": (
            "background-color": "fill",
        ),
    ),
));
```
