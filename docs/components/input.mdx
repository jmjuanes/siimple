---
title: "Input"
description: "The universal text input element"
---

# Input

Add the `siimple-input` class to your text-based `<input>` tags (text, password, number and email types).

```html live=true
<label class="siimple-label">Type your email:</label>
<input type="text" class="siimple-input" placeholder="you@company.com">
```

### Variants

Variants for this component can be defined in the `form.input` section of the variants configuration:

```scss
@include siimple.configure(
    $variants: (
        "form.input": (
            "default": (
                "background-color": "fill",
            ),
        ),
    ),
);
```
