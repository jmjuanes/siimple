---
title: Building a Navbar
---

# Building a Navbar

You can create a navbar component using the [toggler component](/components/toggler) and some helpers:

<NavbarExample />

```html
<div class="content has-p-6 has-d-flex has-items-center has-flex-wrap">
    <!-- Navigation brand: always visible at the left side of the navbar -->
    <a class="has-d-block has-mr-auto has-text-lg has-text-current">
        <strong>Navbar</strong>
    </a>
    <!-- Toggler button: only visible in mobile screens -->
    <div id="navbar-toggler" class="toggler tablet:has-d-none"></div>
    <!-- Navigation menu: by default hidden on mobile screens -->
    <div id="navbar-menu" class="tablet:has-d-flex mobile:has-w-full mobile:has-flex-column mobile:has-mt-4 mobile:has-d-none">
        <a class="navlink mobile:has-mb-2">Link 1</a>
        <a class="navlink">Link 2</a>
    </div>
</div>
``` 

> **Tip**: resize your screen to test how navbar works in mobile screens.

Remember that **siimple does not come with any JavaScript**. However, you can use the following JavaScript implementation example that toggles the visibility of the navigation menu when the toggler button is clicked:

```javascript
const menu = document.querySelector("#navbar-menu");
document.querySelector("#navbar-toggler").addEventListener("click", () => {
    navbarMenu.classList.toggle("mobile:has-d-flex");
    navbarMenu.classList.toggle("mobile:has-d-none");
});
```
