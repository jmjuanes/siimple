---
title: "Label"
description: "Style your form labels"
---

# Label

Style your form **label** element adding a `label` class, useful with radio, checkbox and switch elements.

```html live=true
<label for="my-checkbox" class="label">
    <input type="checkbox" class="checkbox has-mr-3" id="my-checkbox" />
    I agree with the terms of service.
</label>
```

### Variants

Variants for this element can be defined in the `forms.label` section of your theme configuration:

```js
export default {
    forms: {
        label: {
            color: "inherit",
            fontWeight: "bold",
        },
    },
};
```
