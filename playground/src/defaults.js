// Default code for playground
export const defaultHtml = ``.trim();

// Default configuration for playground
export const defaultConfig = `
import colors from "siimple/colors";

export default {
    prefix: "",
    scales: {},
    variants: {},
    plugins: [],
};
`.trim();
